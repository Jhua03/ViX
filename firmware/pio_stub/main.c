/* PlatformIO environment placeholder.
 * The real RP2350 firmware is built by the sdk_build custom target using the
 * official Raspberry Pi Pico SDK CMake project in this same directory. */
int main(void) { return 0; }
